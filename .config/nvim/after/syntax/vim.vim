syn match vimMapRhs /.*\ze|\s*".*/                                              " Match comments when using pipe
syn match vimLineComment /|\s*".*$/                                             " Match comments when using pipe

