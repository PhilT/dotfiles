require('telescope').setup({
  defaults = {
    layout_strategy = 'horizontal',
    sorting_strategy = 'ascending',
    layout_config = {
      width = 0.5,
      mirror = true,
      prompt_position = 'top',
      preview_width = 0.498
    }
  }
})
