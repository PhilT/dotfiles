cd $CODE_DIR/$1

[[ -f startup ]] && ./startup

tmux \
  new-session -s ${1//*\//} 'bash --init-file <(echo ". \"$HOME/.bashrc\"; nvim; pwd")' \; \
  split-window -v -l 20 \; \
  split-window -h \; \
  select-pane -t 0 \; \
  attach
